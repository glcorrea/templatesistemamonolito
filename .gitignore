# ---------------------------------------------------------------- #
#                  GERENCIAMENTO DE DEPENDÊNCIAS                     #
# ---------------------------------------------------------------- #

# Diretório de módulos Node.js (não deve ser versionado, pois pode ser recriado)
node_modules
/node_modules

# Arquivos de lock e cache (exceto o que o Git precisa para lockfileVersion 2+)
/npm-debug.log*
/yarn-debug.log*
/yarn-error.log*
/pnpm-debug.log*

# Diretório de cache de pacotes (geralmente gerado por npm/yarn/pnpm)
/package-lock.json
/npm-shrinkwrap.json
/yarn.lock # Se a versão for antiga e você preferir ignorar
/pnpm-lock.yaml

# ---------------------------------------------------------------- #
#                      ARQUIVOS GERADOS/BUILD                       #
# ---------------------------------------------------------------- #

# Onde o TypeScript compila o seu código
# Substitua 'dist' pela sua 'outDir' definida no tsconfig.json, se for diferente
/dist
/build

# Mapas de código-fonte (Source Maps)
# São úteis para debug, mas não devem ser versionados se estiverem na pasta de build
*.js.map
*.d.ts.map

# Arquivos JavaScript gerados (se eles estiverem na mesma pasta dos arquivos .ts)
# É altamente recomendado configurar 'outDir' no tsconfig.json para evitar isso,
# mas se não for possível, adicione esta regra:
# *.js
# !*.config.js # Exceção para arquivos de configuração como webpack.config.js

# ---------------------------------------------------------------- #
#                     CONFIGURAÇÕES LOCAIS/ENV                       #
# ---------------------------------------------------------------- #

# Variáveis de ambiente
.env
.env.*
!.env.example # Não ignore um arquivo de exemplo sem dados sensíveis

# Chaves/Certificados sensíveis (se armazenados localmente)
/secrets
/keys

# ---------------------------------------------------------------- #
#                       EDITORES E SISTEMAS                          #
# ---------------------------------------------------------------- #

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
# Arquivos de workspace/sessão do VS Code (ignorados para não poluir o git)
.history/
*.code-workspace

# JetBrains IDEs (WebStorm, IntelliJ)
.idea/

# Sistema Operacional (macOS)
.DS_Store

# Windows
Thumbs.db

# ---------------------------------------------------------------- #
#                       TESTES E COVERAGE                          #
# ---------------------------------------------------------------- #

# Diretório de relatório de cobertura de testes (Jest, Istanbul, etc.)
/coverage